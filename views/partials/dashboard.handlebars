<div class="dashView">
    <a href="/dashboard/{{ id }}" class="noteLink">
        <div class="noteTitle">
            <h2>{{ title }}</h2>
            <h2>{{ formatDate dateCreated }}</h2>
        </div>
    </a>
</div>